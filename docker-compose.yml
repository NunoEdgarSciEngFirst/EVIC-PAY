version: "3.9"

services:
frontend:
container_name: evicpay-frontend
build:
context: ./frontend
ports:
- "3000:3000"
volumes:
- ./frontend:/app
- /app/node_modules
environment:
NODE_ENV: development
restart: unless-stopped
